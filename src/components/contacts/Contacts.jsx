import "./Contacts.css"

function Contacts() {

    return(
        <section id="contacts">
            <div className="container">
                <h2 className="section-title">Contact</h2>
            </div>
        </section>
    )
}

export default Contacts 